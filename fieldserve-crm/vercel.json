{
  "buildCommand": "npx expo export -p web",
  "outputDirectory": "dist",
  "routes": [
    { "src": "/__clerk/(.*)", "dest": "/api/clerk?path=$1" },
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
